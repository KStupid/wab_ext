1, send button 
	accessing the button send = document.querySelector('button[aria-label="Send"]');
	to click the element so as to send the message = document.querySelector('button[aria-label="Send"]').click(); 
	
	or
	document.querySelector('span[data-icon="wds-ic-send-filled"]').closest('button');

2, text area	
	acessing the text area 
		document.querySelector('div[data-testid="conversation-compose-box-input"][contenteditable="true"]')
		or 
		document.querySelector('div[data-testid="conversation-compose-box-input"])
		or 
		document.querySelector('div[role="textbox"][contenteditable="true"][aria-placeholder="Type a message"]')

3, chat search box 
	document.querySelector('input[aria-label="Search or start a new chat"]')
	or 
	document.querySelector('input[placeholder="Search or start a new chat"]')
	or
	document.querySelector('input[role="textbox"][aria-label="Search or start a new chat"]')

4, new chat button
	document.querySelector('span[data-testid="new-chat-outline"]')?.closest('button') 
        or
	document.querySelector('span[data-icon="new-chat-outline"]')?.closest('button')

5, entering the number in text area inside new chat button
	document.querySelector('input[role="textbox"][aria-label="Search name or number"]')
	


	yeahh fucking great 

var inp = document.querySelector('input[aria-label="Search name or number"]');

var props = inp[
    Object.keys(inp).find(k => k.startsWith("__reactProps"))
];

var setter = Object.getOwnPropertyDescriptor(
    HTMLInputElement.prototype,
    "value"
).set;

setter.call(inp, "9495342265");

props.onChange({
    target: inp,
    currentTarget: inp,
    type: "change"
});

setTimeout(function () {
    inp.dispatchEvent(new KeyboardEvent("keydown", {
        key: "Enter",
        code: "Enter",
        keyCode: 13,
        which: 13,
        bubbles: true
    }));

    inp.dispatchEvent(new KeyboardEvent("keyup", {
        key: "Enter",
        code: "Enter",
        keyCode: 13,
        which: 13,
        bubbles: true
    }));
}, 300);
